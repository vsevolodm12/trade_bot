## Техническое задание: Telegram-бот для уведомлений о ценах акций (РФ и США)

### 1. Цель проекта

Создать персонального помощника в Telegram, который в фоновом режиме отслеживает котировки заданных ценных бумаг и мгновенно уведомляет пользователя, когда цена достигает установленного «таргета».

### 2. Интерфейс и навигация (UI/UX)

Взаимодействие строится на **Inline-кнопках** (кнопки под сообщениями) и **меню**, чтобы минимизировать ручной ввод текста.

**Главное меню (постоянные кнопки):**

* ➕ **Добавить уведомление** — запуск процесса создания нового алерта.
* 📊 **Мой портфель (Алерты)** — список всех активных «слежек».
* 📈 **Текущие цены** — быстрая проверка цен по списку из портфеля.
* ⚙️ **Настройки** — выбор частоты проверки (например, раз в минуту или раз в 5 минут).

---

### 3. Функциональные этапы работы

#### А. Добавление новой акции

1. Пользователь нажимает «Добавить уведомление».
2. Бот просит прислать **тикер** (краткое название, например: `SBER` или `AAPL`).
3. **Проверка:** Бот обращается к API и возвращает карточку:
> **Найдено: Сбербанк (MOEX)**
> Текущая цена: 285.5 руб.
> *Введите целевую цену (число):*


4. Пользователь вводит цифру (например, `350`).
5. Бот подтверждает: «Принято! Маякнем, когда цена достигнет 350 руб.».

#### Б. Система уведомлений (Триггер)

* Бот в фоновом режиме опрашивает биржи через API с заданной периодичностью.
* При достижении или пересечении целевой цены бот присылает сообщение:
> 🔔 **АЛЕРТ: SBER**
> Цель: **350.00** | Факт: **350.05**
> Пора принимать решение!
> [Кнопки: «Удалить алерт» | «Переставить выше» | «Открыть график»]



#### В. Управление портфелем

* В разделе «Мой портфель» отображается список всех тикеров и их целей.
* Под каждой позицией — кнопка «Удалить», чтобы быстро почистить список.

---

### 4. Техническая часть (Источники данных)

Для обеспечения работы необходимо интегрировать:

1. **Российский рынок (MOEX):**
* Используется протокол **ISS MOEX**.
* Данные берутся по акциям, торгуемым в режиме основных торгов.
* Бесплатный режим обеспечивает данные с задержкой 15 минут, чего достаточно для большинства задач.

Публичный доступ (Delayed): Для получения данных с задержкой в 15 минут регистрация и API-ключ не требуются. Вы просто отправляете GET-запросы на URL вида https://iss.moex.com/iss/... и получаете ответ в формате JSON или XML.

(документация - https://www.moex.com/a7939 / https://iss.moex.com/iss/reference/?lang=en
)
2. **Рынок США и Гонконга (NYSE, NASDAQ):**
* Рекомендуется использование **Twelve Data** 
* Эти сервисы имеют бесплатные лимиты (до 800 запросов в сутки), которых хватит для личного пользования 2–3 человек.

---

### 5. Требования к надежности

* **База данных:** Бот должен «помнить» все установленные алерты даже после перезагрузки сервера.
* **Скорость:** Проверка цен должна происходить не реже одного раза в 1–2 минуты в рабочее время бирж.
* **Уведомления:** Бот должен использовать Push-уведомления Telegram, чтобы сообщение всплывало на заблокированном экране телефона.


5. Мультивалютный учет и Конвертация

Поддержка валют: Бот должен корректно обрабатывать и отображать символы валют в зависимости от биржи: RUB (РФ), USD (США), HKD (Гонконг).

Авто-определение: При вводе тикера бот автоматически подтягивает валюту из API, исключая ошибку ручного ввода (например, нельзя поставить цель 350 долларов для Сбербанка, если он торгуется в рублях).

Рекомендованная частота (polling)

Исходя из опыта и ваших задач, оптимально настроить так:

РФ: Раз в 1 минуты. Это обеспечит актуальность даже с учетом 15-минутной задержки самой биржи и не перегрузит ваш сервер.

США / Гонконг: Раз в 3–5 минут (в бесплатном режиме). Этого достаточно, чтобы «держать руку на пульсе» и не вылететь за лимиты API.

БД - Sqlite

Дополнения:
Пункт ТЗ: Архитектура сбора данных и оптимизация лимитов

1. Гибридная система источников (Multi-source Data Fetching):

Российский рынок (MOEX): Прямые запросы к MOEX ISS API. Используется публичный доступ (задержка 15 мин), лимиты на количество запросов практически не ограничены для персонального использования.

Международные рынки (США, Европа, Гонконг):

Основной источник (Primary): Бесплатный агрегатор (библиотека yfinance или аналоги). Используется для массового мониторинга всего портфеля (100+ тикеров) без ограничений по количеству запросов и кредитов.

Дополнительный источник (Secondary/Twelve Data): Используется для высокоприоритетных бумаг (например, топ-10 акций в портфеле) или как резервный канал.

2. Логика управления лимитами Twelve Data:

Для экономии кредитов (лимит 800/день) бот должен использовать Batch Requests (пакетные запросы), объединяя несколько тикеров в один запрос.

Бот должен автоматически переключаться на бесплатный агрегатор, если суточный лимит Twelve Data (800 кредитов) исчерпан на 90%.

Запросы по международным рынкам должны выполняться только в часы работы соответствующих бирж (США, Европа, Гонконг), чтобы не тратить лимиты впустую ночью или в выходные.

3. Обработка валютных пар (EUR, USD, HKD):

Для автоматической конвертации цен в рубли бот должен раз в 30 минут подтягивать актуальные курсы валют через MOEX ISS (пара USD/RUB, EUR/RUB).

Все цены в базе данных хранятся в «родной» валюте тикера, а конвертация для отображения происходит «на лету» по последнему сохраненному курсу.